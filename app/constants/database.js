const database = {
    HOST: 'localhost',
    USER: 'root', // Change credentials
    PASSWORD: '1234', // Change credentials
    DATABASE: "db_pmo_dev",
    TIMEOUT: 20000
}

module.exports = {
    database
}