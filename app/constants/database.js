const database = {
    HOST: 'localhost',
    USER: 'root', // Change credentials
    PASSWORD: 'root', // Change credentials
    DATABASE: "db_pmo",
    TIMEOUT: 20000
}

module.exports = {
    database
}